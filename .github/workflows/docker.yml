name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read
  packages: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Cache NuGet packages
        uses: actions/cache@v4
        with:
          path: ~/.nuget/packages
          key: nuget-${{ runner.os }}-${{ hashFiles('**/*.csproj') }}
          restore-keys: nuget-${{ runner.os }}-

      - name: Format check
        run: |
          dotnet format OrbitBreak.Client/OrbitBreak.Client.csproj --verify-no-changes
          dotnet format OrbitBreak.Tests/OrbitBreak.Tests.csproj --verify-no-changes

      - name: Build client
        run: dotnet build OrbitBreak.Client/OrbitBreak.Client.csproj -c Release

      - name: Run tests
        run: dotnet test OrbitBreak.Tests/OrbitBreak.Tests.csproj -c Release

  push-image:
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push client image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: OrbitBreak.Client/Dockerfile
          push: true
          tags: ghcr.io/alon-shviki/orbit-break-client:latest
